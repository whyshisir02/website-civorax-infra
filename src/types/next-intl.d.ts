// Intentionally no AppConfig augmentation.
// next-intl useTranslations will use the default Record<string, any> Messages type.
// Strict type-safe translation keys can be enabled here once next-intl TypeScript
// augmentation is confirmed to work with the project's specific translation structure.
export {};
